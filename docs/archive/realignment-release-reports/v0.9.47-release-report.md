# Release Completion Report — v0.9.47

Maintenance release. Two more 400→404 sites: Apple operations cancel and Forsetti module state.

## Scope completed

### Two more "missing-resource" routes return 404

| Route                                                  | pre-v0.9.47 | v0.9.47 |
|---|---|---|
| `POST /api/clu/apple-operations/cancel` (unknown id)   | 400         | **404** |
| `POST /api/forsetti/modules/state` (unknown moduleId)  | 400         | **404** |

The apple-operations/cancel route uses a substring match on `"was not found."` because the helper embeds the operationId in quotes (`"Apple operation 'foo' was not found."`). The forsetti modules state route uses an exact-message match because the message is fixed (operator selects from a fixed module catalog).

**Verified live**:

```
=== v0.9.47 ===
POST /api/clu/apple-operations/cancel  body:{"operationId":"never"}  -> 404
POST /api/forsetti/modules/state       body:{"moduleId":"never","action":"enable"}  -> 404

=== regression sweep — every "missing resource" route now 404 ===
GET    /api/version                                        -> 200
DELETE /api/clients/never                                  -> 404 (v0.9.37)
POST   /api/clients/never/privileges                       -> 404 (v0.9.46)
POST   /api/platform-services/apple-hosts/remove unknown   -> 404 (v0.9.45)
POST   /api/runtime/mcp-servers/remove unknown             -> 404 (v0.9.38)
POST   /api/leases/never/release                           -> 404 (v0.9.33)
GET    /api/pools/never                                    -> 404 (v0.9.30)
tools/list via /mcp                                        -> 33 tools (18991 bytes)
/api/health/summary                                        -> ok:true, toolCount:33
```

The 400-vs-404 sweep that started in v0.9.30 (`/api/pools/{unknown}`) and accumulated through v0.9.33, v0.9.37, v0.9.38, v0.9.43, v0.9.45, v0.9.46, and v0.9.47 is now complete across every route I've probed — every "resource doesn't exist" surface returns 404 with a JSON body identifying what wasn't found.

## Files changed

| File | Change |
|---|---|
| [`src/MasterControlApp/MasterControlRuntime.cpp`](../../../src/MasterControlApp/MasterControlRuntime.cpp) | Two route handlers gained the standard `result.succeeded ? 200 : (message-matches-not-found ? 404 : 400)` ternary. |
| [`VERSION.json`](../../../VERSION.json) | 0.9.46 → 0.9.47. |

## Validation performed

| Command | Result |
|---|---|
| `cmake --build` | passed |
| `ctest -C Debug` | 4/4 passed |
| `scripts/check-mastercontrol-forsetti.ps1` | passed |
| 2× new 404 probes | both 404 with diagnostic body |
| Regression sweep: 7 prior 404 routes | all unchanged |
| `serverInfo.version="0.9.47"` | PASS |

## Deferred work

- **MCOS sub-agent runner** (architectural decision; carried).
- **PHASE-13 Win2D rendering** (UI work; carried).
- **SSE upgrade for Streamable HTTP** (significant feature; carried).

## Ready for next phase?

Yes. **44 iterations shipped this session (v0.9.4 → v0.9.47).** Continuing.
