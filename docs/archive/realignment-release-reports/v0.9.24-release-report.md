# Release Completion Report — v0.9.24

Maintenance release. Adds a minimal version probe.

## Scope completed

`GET /api/version` returns `{ version, time }` — lighter than `/api/health/summary` for monitoring tools that just want to detect a version flip on deploy. Three lines of route handler.

```
GET /api/version → { "version": "0.9.24", "time": "2026-05-09T20:15:20Z" }
```

The v0.9.24 bug-hunt also confirmed:
- 256 KB tools/call request payload round-trips intact (HTTP.sys + entity-body draining handles it)
- Filesystem MCP correctly enforces sandbox (rejects path outside the rooted directory with "Access denied - path outside allowed directories")
- Governance bundle checksums are stable across re-fetches (idempotent)
- Fresh-client end-to-end flow: discovery → onboarding → tools/list (33 tools) → tools/call → real Win32 hostname (PC-GAMING-R7-5800X) all work via the discovered URLs without any out-of-band knowledge

## Files changed

| File | Change |
|---|---|
| [`src/MasterControlApp/MasterControlRuntime.cpp`](../../../src/MasterControlApp/MasterControlRuntime.cpp) | New `GET /api/version` route. |
| [`VERSION.json`](../../../VERSION.json) | 0.9.23 → 0.9.24. |

## Validation performed

| Command | Result |
|---|---|
| `cmake --build` | passed |
| `ctest -C Debug` | 4/4 passed |
| `scripts/check-mastercontrol-forsetti.ps1` | passed |
| Live `GET /api/version` | returns `{version: "0.9.24", time: ...}` |
| 256 KB tools/call payload round-trip | preserved exactly |
| `serverInfo.version="0.9.24"` | PASS |

## Deferred work

- **MCOS sub-agent runner** (carried).
- **PHASE-13 Win2D rendering** (carried).
- **SSE upgrade** (carried).

## Ready for next phase?

Yes. **21 iterations shipped this session (v0.9.4 → v0.9.24).** Continuing.
