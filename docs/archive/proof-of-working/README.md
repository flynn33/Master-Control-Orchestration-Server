# Archived proof-of-working receipts

**Historical evidence — not current instructions.** These are the 2026-04 feature verification receipts: numbered proof documents (`01-multi-binary.md` … `18-apple-host-probe.md`), raw probe outputs (`mcos_proof_*.json` / `.txt` / `.html`), the concatenated receipts document (`PROOF-OF-WORKING.md`), and the original receipts contract (`original-receipts-README.md`, formerly this directory's `README.md`). Everything came from `plans/PROOF-OF-WORKING/`.

The receipts capture the product as verified in April 2026 (pre-v0.11.0). Several features they exercise have since been reworked — notably the gateway substrate and diagnostics module — so treat them as point-in-time evidence, not as test procedures. Current verification lives in the test suite (`tests/`) and the repository gates (`scripts/Invoke-MCOSRepositoryHealth.ps1`, `scripts/Invoke-MCOSRemediationGates.ps1`).
