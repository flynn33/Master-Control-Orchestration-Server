{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "comment": "Local-only settings layered on top of .claude/settings.json. Project-scoped overrides + machine-specific behaviors."
}
