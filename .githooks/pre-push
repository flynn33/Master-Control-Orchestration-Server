#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
refs_payload="$(cat)"

if [ -z "$refs_payload" ]; then
  exit 0
fi

printf '%s\n' "$refs_payload" | powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$repo_root/scripts/Invoke-AiContributorGuard.ps1"
